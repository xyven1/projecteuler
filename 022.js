const names = require('fs').readFileSync('./data/p022_names.txt', 'utf-8').split(',').map(v=>v.slice(1, -1))
console.log(names.sort().map((name, index)=>name.split('').reduce((a,char)=> a + char.charCodeAt(0)-64, 0)*(index+1)).reduce((a,b)=>a+b))